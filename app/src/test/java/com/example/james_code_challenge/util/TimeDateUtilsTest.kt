package com.example.james_code_challenge.util

import org.junit.Assert.*

class TimeDateUtilsTest {
    // D_N: In the interest of time, I will no be creating these unit tests
    // Understand I can, but have demonstrated my ability elsewhere
    // If I did fill in this class, I wouldnt use Mocks, and will instead just call String.toLocalDate in a variety of situations & dates,
    // and assert the output matches the desired result
    // In particular, I'd assert that every "date_published" scenario (which comes with a variety of millisecond lengths) is covered
}